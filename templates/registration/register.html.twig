{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <section class="bg-gray-50 dark:bg-gray-900 h-full">
        <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto h-full lg:py-0">
            <a class="flex items-center mb-6 text-2xl font-semibold text-gray-900 dark:text-white">
                Register to EduEssentials
            </a>
            {{ component('RegistrationForm', {
                attributes: {
                    class: 'w-full bg-white rounded-lg shadow dark:border p-4 md:mt-0 sm:max-w-md  dark:bg-gray-800 dark:border-gray-700'
                },
            }) }}
        </div>
    </section>
{% endblock %}
